```bash
flag needs an argument: 'h' in -h
Usage of ./oracleDump:
      --expCols strings   output column names, optional, columus count must be same as select query, default is same as select query. e.g. --expColNames="col1,col2,col3"
      --expTab string     output table name, required
      --expUser string    output user name
  -h, --host string       host (default "localhost")
  -o, --outFile string    output file name (default "insert.sql")
  -p, --password string   password
  -P, --port int          port (default 1521)
  -s, --service string    service
  -q, --sql string        select sql
      --url string        oracle url, e.g. oracle://user:pass@server/service_name
  -u, --username string   username to login (default "dbsel")
  -v, --version           show version
flag needs an argument: 'h' in -h
```